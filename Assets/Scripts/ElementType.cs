using UnityEngine;

// |EN| Enum representing different elemental damage types |TR| Farklı element hasarı türlerini temsil eden enum

public enum ElementType
{
    None,
    Fire,
    Ice,
    Lightning
}
